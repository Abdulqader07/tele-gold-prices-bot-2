[project]
name = "gold-price-bot"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "flask>=3.1.3",
    "python-dotenv>=1.2.2",
    "requests>=2.33.1",
    "supabase>=2.30.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
